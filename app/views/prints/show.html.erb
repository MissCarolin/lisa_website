 <%= render 'shared/header' %>
      <div class="container-show">
        <div class="content-show">
            <%= cl_image_tag(@print.photo.key, crop: :fill, width: 500, class:"image-show") %>
            <div class="show-name-description">
                <h1 class="headline-show"><%= @print.name %></h1>
                <p class="image-pictures-description"><%= @print.description %></p>
            </div>
        </div>
    </div>