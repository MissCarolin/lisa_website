 <%= render 'shared/header' %>
    <div class="container_impressum">
        <div class="content_impressum">
            <h1 class="headline-show"><%= @print.name %></h1>
            <div class="show-content">
                <%= cl_image_tag(@print.photo.key, crop: :fill, width: 400) %>
                <p class="image-pictures-description"><%= @print.description %></p>
            </div>
        </div>
    </div>